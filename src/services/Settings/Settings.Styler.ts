export namespace Styler {
	export const UpperCasing = (text: string): string => text.toUpperCase()
	export const LowerCasing = (text: string): string => text.toLowerCase()
	export const returnStyle = (text: string): string => text
}
